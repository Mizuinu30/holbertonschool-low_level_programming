#include <stdio.h>

/**
 * main - writes a program that prints all single digit #
 * of base 10 starting form 0.
 *
 * Return: Always 0.
 */

int main(void)
{
	int number;


	for (number = 0; number < 10; number++)
	{
		printf(number);
	}

	return (0);
}
